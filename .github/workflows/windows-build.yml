name: windows-build

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: windows-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --target x86_64-pc-windows-msvc -j 4

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: bevy-demo-windows
          path: target/x86_64-pc-windows-msvc/release/bevy-demo.exe
          if-no-files-found: error
